---
# CKV_GHA_7 (workflow_dispatch inputs must be empty) is unsatisfiable for
# .github/workflows/gitstream.yml: the file is generated by the gitStream
# GitHub App and its inputs are the app's dispatch contract. Documented in
# docs/APP_STANDARDS.md (Security exceptions). No other workflow in this
# repo uses workflow_dispatch inputs; new workflows must not either.
skip-check:
  - CKV_GHA_7
